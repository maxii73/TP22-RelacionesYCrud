<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= actor.first_name %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>    
    <br>
    <p>     
        <a class="botonVolver" href="/">inicio</a> 
        <a class="botonAgregar" href="/actors">Actores</a>    
    </p>
    <br>
    <h1><%= actor.last_name %>, <%= actor.first_name %> </h1>
    <br>
    <h2>Peliculas</h2>
    <p>NAME: <%= actor.first_name %> </p>
    
    <% if (actor.movies.length) { %>
        <ul>
            <% actor.movies.forEach(movie => { %>
                <li><a href="/movies/detail/<%= movie.id %> "><%= movie.title %> </a></li>
                <% }) %>  
        </ul>
     
    <% }else{ %>
        <p>El actor no tiene peliculas</p>
        <% } %>

</body>
</html>